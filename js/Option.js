$(".singleMessage").click(function () {
    // switch activation / désactivation div .messageOption
    if
        // afficher la div


        // fonction copie
    $( ".copy").click(function() {
        $( "" ).clone();
    });
        // api google translate
    $( ".translate").click(function() {
        $( "" ).select();
    });
    // fonction correction, récupère le message, en fait une copie dans un champs non modifiable et une dans un champs texte
    $( ".correct").click(function() {
        $( "" ).select();
    });

    // si la div est déjà affichée, la cachée
})

